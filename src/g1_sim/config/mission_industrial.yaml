# GENERATED BY scripts/gen_missions.py — do not edit by hand.
# World: industrial — industrial warehouse (convex, scattered obstacles)
#
#   ros2 launch g1_sim g1_a_star_mpc.launch.py world:=industrial \
#        use_mission:=true \
#        mission_file:=$(ros2 pkg prefix g1_sim)/share/g1_sim/config/mission_industrial.yaml
#
# reach_radius > goal_reached_radius (0.25) of the G1 profile: below that value
# a_star_node stops replanning, so a tighter threshold would stall the mission one
# step short of the finish.
frame_id: odom
reach_radius:     0.35
leg_timeout_sec: 280.0

waypoints:
  - { x:  10.00, y:   0.00, yaw: 0.0, name: "goal" }
